program main(input);
	var a,b: integer;
	var x,y: real;
	var c: array[1 .. ] of integer;
	function foo(x:integer):integer;
	begin
		foo := foo(b)
	end;
	procedure boo(y:integer);
		procedure foo(b:real,c:real);
		begin
		end;
	begin
		foo(1,2)
	end;
	procedure read(a: array[1..] of integer);
	begin
	end;
begin
	a := b + c[foo(c[foo(3)])];
	read(@b)
end.
